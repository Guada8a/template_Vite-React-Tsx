import Container from '@mui/material/Container';

function FooterApp() {
    return (
        <footer>
            <Container maxWidth="lg"></Container>
        </footer>
    );
}

export default FooterApp;
